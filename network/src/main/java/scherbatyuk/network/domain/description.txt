Потрібно реалізувати:
5) додати створення груп
6) реалізувати інтернаціональність
7) реалізувати переклад на різні мови діяльність користувачів (преміум)
перевірка дійсної електронки через код
відправлення повідомлення на електронку

запобіжник виправити додавання лайків тим самим користувачем


ВИКОНАВ!
3) сторінка із повідомленнями, реалізувати можливість відповіді, сортування повідомлень
(можливо додати сотрування)???
4) налаштувати пошук користувачів за фільтрами
2) відображення постів - доопрацювати:
   - видалити пост через модальне вікно,
   - лайк (без зміни іконки на початку)
   - коментування(окреме вікно),
   - хедер із власником та датою створення
   - репост,
8) іконка з рейтингом:
   - найпопулярніший твій допис,
   - найпопулярніший допис друзів
4) налаштувати пошук дописів за тегом
1) реалізувати активність користувача

@GetMapping("/search/autocomplete")
@ResponseBody
public List<String> searchAutocomplete(@RequestParam String term) {
    // Поверніть список рядків, які відповідають терміну пошуку.
    // Це можуть бути імена користувачів, хештеги тощо.
    return searchService.searchAutocomplete(term);
}
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
$(document).ready(function() {
    $('.form-control').on('input', function() {
        var searchTerm = $(this).val();
        $.get('/search/autocomplete', { term: searchTerm }, function(data) {
            // Оновіть віджет автозаповнення з даними, отриманими від сервера.
            // Це може залежати від використовуваного вами плагіна автозаповнення.
        });
    });
});
</script>